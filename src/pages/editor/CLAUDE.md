# CLAUDE.md

이 디렉터리(에디터 도메인)의 작업 규칙은 같은 위치의 `AGENTS.MD`에 정리되어 있습니다.
Claude는 `AGENTS.MD`를 기본으로 읽지 않으므로, 아래 import로 읽어 들입니다.
에디터 작업은 전역 규칙(최상위 `CLAUDE.md` / `AGENTS.MD`)과 이 도메인 규칙을 함께 따릅니다.

## 에디터 도메인 규칙

@AGENTS.MD

## 프로젝트 구조 가이드

@ARCHITECTURE.md
